import 'package:flutter/material.dart';

class AgentPageView extends StatelessWidget {
  const AgentPageView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(child: Text("Agent Page")),
    );
  }
}
